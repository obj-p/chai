# direnv configuration - automatically loads .env when entering directory
# Install: brew install direnv
# Setup: add `eval "$(direnv hook zsh)"` to ~/.zshrc (or bash equivalent)
# Usage: run `direnv allow` after creating .env
dotenv_if_exists
