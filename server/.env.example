# Chai Server Configuration
#
# Copy this file to .env and uncomment/modify values as needed.
# Environment variables are overridden by command-line flags.
#
# Precedence (highest to lowest):
#   1. Command-line flags (e.g., -port 3000)
#   2. Environment variables (e.g., CHAI_PORT=3000)
#   3. Default values

# HTTP port (default: 8080)
# CHAI_PORT=8080

# SQLite database path (default: chai.db)
# If relative, resolved against CHAI_WORKDIR
# CHAI_DB=chai.db

# Working directory for Claude CLI (default: current directory)
# Claude CLI processes will run with this as their working directory
# CHAI_WORKDIR=/path/to/project

# Path to Claude CLI command (default: claude)
# CHAI_CLAUDE_CMD=claude

# Timeout for prompt requests (default: 5m)
# Uses Go duration format: 30s, 5m, 1h, etc.
# CHAI_PROMPT_TIMEOUT=5m

# Graceful shutdown timeout (default: 30s)
# Time to wait for in-flight requests before force shutdown
# CHAI_SHUTDOWN_TIMEOUT=30s
